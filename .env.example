# Existing environment variables...

# AI Groups Token Optimization (Optional)
AI_GROUPS_MAX_PARALLEL_AGENTS=3        # Max agents to run in parallel
AI_GROUPS_TIMEOUT_MS=20000             # Timeout per agent (ms)
AI_GROUPS_MAX_OUTPUT_LENGTH=3000       # Max chars per agent response
AI_GROUPS_TOKENS_PER_AGENT=500         # Estimated tokens per agent
AI_GROUPS_MAX_TOKENS=2000              # Total token budget per request
AI_GROUPS_CHUNK_DELAY=20               # Streaming delay (ms)
AI_GROUPS_SMART_PRIORITY=true          # Enable smart agent prioritization
AI_GROUPS_PREFER_SHORT_PROMPTS=true    # Prefer agents with shorter prompts
AI_GROUPS_TOKEN_RATIO=4                # Characters per token ratio